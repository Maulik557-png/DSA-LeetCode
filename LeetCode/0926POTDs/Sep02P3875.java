public class Sep02P3875 {
    // If all are odd -> true
    // If all are even -> true
    // If odd + even, we can always convert it to odd -> true
    public boolean uniformArray(int[] nums1) {
        return true;
    }
}
